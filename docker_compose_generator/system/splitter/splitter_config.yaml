build:
  context: ./src/
  dockerfile: aggregation/Dockerfile
depends_on:
  rabbitmq:
    condition: service_healthy
environment:
- MOM_HOST=rabbitmq
- PYTHONUNBUFFERED=1
volumes:
- ./worker_logs:/worker_logs