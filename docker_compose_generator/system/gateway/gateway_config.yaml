build:
  context: ./src
  dockerfile: gateway/Dockerfile
container_name: gateway
depends_on:
  rabbitmq:
    condition: service_healthy
environment:
- MOM_HOST=rabbitmq
- PYTHONUNBUFFERED=1
- SERVER_HOST=gateway
- SERVER_PORT=7659
- GATEWAY_OUTPUT_BATCH_SIZE=5000
